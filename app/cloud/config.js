const config = {
    dev: {
        protocol: 'http',
        host: 'localhost',
        port: 8000
    },
    release: {
        protocol: 'http',
        host: 'note.pwp.app',
        port: 8000
    }
};

module.exports = config;